<nav class="navbar tsticky-top navbar-expand-md navbar-dark bg-dark">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand site-title" href="#">JOSMANOA.com</a>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('josmanoa_home') }}">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">My Account</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('josmanoa_index_album') }}">Albums</a>
            </li>
        </ul>
    </div>
    <div class="nav-item dropdown mr-10 text-white">
        {% if not app.user %}
            <div class="btn-group">
                <button type="button" class="btn btn-light bt-xs dropdown-toggle dropdown-header" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" id"toto">
                    <i class="fa fa-lock-open"></i> Sign in
                </button>

                <div class="dropdown-menu dropdown-menu-right">
                    {{ render(controller('App\\Controller\\SecurityController::login')) }}
                </div>
            </div>
        {% else %}
            <a href="{{ path('logout') }}" class="btn btn-light btn-xs">
                <i class="fa fa-lock"></i> Logout
            </a>
        {% endif %}
    </div>
</nav>